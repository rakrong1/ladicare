{
  "name": "hair-product-store",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd frontend && npm install --unsafe-perm && npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "dependencies": {
    "react-icons": "^5.5.0",
    "vite": "^6.3.5"
  }
}
